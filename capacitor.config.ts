import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nagendrazspace.tradeanalyst',
  appName: 'TradeAnalyst Pro',
  webDir: 'dist'
};

export default config;
